import React from 'react';
import Hero from '../Hero/Hero';

const Home = () => {
    return (
        <div className="home">
            {/* === Hero Section === */}
            <Hero />
        </div>
    );
};

export default Home;